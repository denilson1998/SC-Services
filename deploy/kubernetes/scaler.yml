apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-scaler
  namespace: "#{{Namespace}}#"
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: apidelivery-deployment  
  minReplicas: #{{MinReplicas}}#
  maxReplicas: #{{MaxReplicas}}#
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: #{{AverageUtilizationCpu}}#
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: #{{AverageUtilizationMemory}}#
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: apipayments-scaler
  namespace: "#{{Namespace}}#"
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: apipayments-deployment  
  minReplicas: #{{MinReplicas}}#
  maxReplicas: #{{MaxReplicas}}#
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: #{{AverageUtilizationCpu}}#
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: #{{AverageUtilizationMemory}}#
---
